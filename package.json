{
  "name": "ara",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,worker -c blue,green,yellow \"bun run dev:api\" \"bun run dev:web\" \"bun run dev:worker\"",
    "dev:api": "bun run --cwd apps/api dev",
    "dev:web": "bun run --cwd apps/web dev",
    "dev:worker": "bun run --cwd apps/worker dev",
    "dev:cli": "bun run --cwd apps/cli dev",
    "start": "concurrently -n api,web,worker -c blue,green,yellow \"bun run start:api\" \"bun run start:web\" \"bun run start:worker\"",
    "start:api": "bun run --cwd apps/api src/index.ts",
    "start:web": "bun run --cwd apps/web preview --host 0.0.0.0 --port 5173",
    "start:worker": "bun run --cwd apps/worker src/index.ts",
    "build": "bun run --cwd apps/api build && bun run --cwd apps/web build && bun run --cwd apps/cli build",
    "build:cli": "bun run --cwd apps/cli build",
    "typecheck": "tsc --noEmit",
    "typecheck:cli": "bun run --cwd apps/cli typecheck",
    "test": "bun test",
    "lint": "tsc --noEmit",
    "clean": "rm -rf apps/*/dist packages/*/dist node_modules",
    "setup": "bun run scripts/setup.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}